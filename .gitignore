# Provider sync outputs — regenerated by `node skills/harness-init/scripts/install.ts`
# and `node skills/harness-pair-dev/scripts/sync.ts`.
.claude/
.codex/
.gemini/

# Runtime workspace seeded by install.ts; state.md/events.md/epics mutate per cycle.
.harness/

# Assistant-context pointer docs for working inside this repo. They are
# developer-local aids (Claude Code / Codex CLI / Gemini CLI auto-loads),
# not part of the plugin's public surface.
CLAUDE.md
AGENTS.md
